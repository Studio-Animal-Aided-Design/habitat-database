export default function Loading() {
  return (
    <main className="route-loading" aria-live="polite" aria-busy="true">
      <span />
      <p>Planungswissen wird zusammengestellt …</p>
    </main>
  );
}
